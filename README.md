# Motion-Detection-System
An Arduino Uno project that detects motion using a PIR sensor, activates an LED and buzzer alarm, and displays the last detected motion time on a 16×2 I²C LCD using a user-defined reference time.
